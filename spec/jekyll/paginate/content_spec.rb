RSpec.describe Jekyll::Paginate::Content do
  it "has a version number" do
    expect(Jekyll::Paginate::Content::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
